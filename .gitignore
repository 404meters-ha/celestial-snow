# 密钥与本地配置——绝不提交
.env

# Python
__pycache__/
*.pyc
.venv/

# 数据与构建产物
*.db
*.db-shm
*.db-wal
frontend/node_modules/
# frontend/dist 入库：服务器 git clone 即可直接启动（构建产物 1.4M，改版后重新 build+commit）
courses/

# vendored cc-mini 的运行时状态（技能缓存等，可再生）
.cc-mini/

# 课程发布目录（LOCAL_PUBLISH_DIR 的落盘产物，可随时重新发布）
published/

# IDE
.idea/

# Claude 本地配置（技能 .claude/skills/ 会入库，本地设置不入）
.claude/settings.local.json
scaffolds/
doc/scaffold/*.log
